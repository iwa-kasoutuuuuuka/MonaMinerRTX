# GEMINI.md - 開発規範・思考プロセス・デバッグガイドライン

本ドキュメントは、本プロジェクト（**MonaMiner RTX / RX**）における開発思考プロセス、アーキテクチャ設計原則、および構造化デバッグ指針を網羅的に定義するものです。  
本プロジェクトに関わるすべてのAIエージェントおよび開発者は、本ドキュメントの規定に従って思考・実装・検証を行ってください。

---

## Ⅰ. 思考プロセスに関する指示 (Thinking Methodology)

回答・コード生成・アーキテクチャ選定を行う前に、バックグラウンドで必ず以下のステップを踏んで思考・自己検証を行ってください。

### 1. ユーザーの意図と背景にある本質的な課題の抽出 (Intent & Root Needs)
* **表層要求に惑わされない**: 単に「ボタンを動かす」「エラー文字を消す」のではなく、ユーザーが真に達成したい目的（例: 発熱を抑えながら最高効率で採掘したい、離席時だけ自動で掘りたい、外部ソフトなしで完結させたい）を明確化する。
* **物理制約・環境要件の把握**:
  - ハードウェア制約: NVIDIA Blackwell (CC 12.0) / Ada Lovelace / Ampere、AMD RDNA 3/2、VRAM容量、PCIe帯域、TDP（電力許容範囲）。
  - アルゴリズム制約: Lyra2REv2（Blake256 -> Keccak256 -> CubeHash -> Lyra2 -> Skein -> BMW）のメモリ・レジスタ・並列演算特性。
  - OS・権限制約: Windows 10/11 (64bit)、一般権限 vs 管理者権限（NVML直接制御の可否）、cp932/UTF-8の文字コード。

### 2. 複数の異なるアプローチや仮説のリストアップ (Minimum 3 Approaches)
単一の解決策に固執せず、最低3つの異なる技術スタック、アーキテクチャパターン、制御ロジックを並列に検討・提示する。

### 3. 各仮説のメリット・デメリット、論理的矛盾の自己検証 (Trade-off Analysis)
* 性能（ハッシュレート / 消費電力比）、コード保守性、外部依存の有無（アンチウイルス誤検知リスク）、安全性をマトリクス比較する。
* 「一見良さそうだが、Windows環境やマルチGPUで破綻しないか」といった論理的矛盾を徹底的に自己検証する。

### 4. 最も合理的かつ網羅的な結論の導出 (Optimal Synthesis)
* 現実の実行環境（RTX 5080、Python 3.10、PySide6、Windows OpenCL.dll）において、最も副作用が少なく拡張性の高い解決策を選定する。

### 5. 段階的なステップ検証 (Step-by-Step Staged Execution)
* 一足飛びにコードを書き換えず、段階的にステップに分けて論理的に検証した上で回答・実装を行う。
  1. インターフェース設計・引数定義
  2. コア演算・通信プロトコル実装
  3. UIバインディング・シグナル接続
  4. 単体テスト・自己診断ツール（`diagnose.py`）での自動検証

### 6. あえて批判的な視点（悪魔の代弁者）による検証 (Devil's Advocate & Anti-Hallucination)
* **ハルシネーション（嘘・幻覚）の完全根絶**:
  - 実在しないライブラリ関数、非現実的なハッシュレート値、サポートされていないGPU命令（例: Lyra2REv2でTensorコアが使える等）を徹底排除。
* **極限状態のストレステスト想定**:
  - プール切断時、マイナー強制終了時、高DPI拡大時、無効なモナコインアドレス入力時にクラッシュせず安全に復旧できるかを批判的にチェックする。

---

## Ⅱ. デバッグに関する指示とアプローチ (Debugging Guidelines)

Anti-Gravity 環境におけるエラーの発生や予期しない挙動に対しては、当てずっぽうな修正を避け、以下の**構造化されたデバッグプロセス**に従って思考・対処してください。

```
[事象の明確化] (エラーログ・差異の特定)
       │
       ▼
[エラーの局所化] (UI / Stratum通信 / OpenCLカーネル / ハードウェア層の分離)
       │
       ▼
[再現手順の確立] (最小限のスクリプトで100%再現)
       │
       ▼
[推論と仮説検証] (複数の仮説を立て、ログ・モックで1つずつ反証)
       │
       ▼
[最小限のクリーンな修正] (無関係なリファクタリングを混在させない)
       │
       ▼
[退行（デグレード）防止] (diagnose.py 実行 & 配布ZIP再検証)
```

### 1. 根本原因の特定 (Root Cause Analysis)
単に「動かない」「エラーが出た」という現象に対処するのではなく、エラーの本質を突き止めます。
* **事象の明確化**: 何が起きているか（エラーログ、WinErrorコード、期待値と実際の出力の乖離）を正確に言語化する。
* **エラーの局所化**: 影響を受けているモジュール、関数、またはコンポーネントを特定し、問題を最小限のコード片に切り離す。
  - 例: `[WinError 10049]` 発生時、ソケット層・URLパース層・GUI入力層のどこで空文字列が発生したかを追跡。
* **再現手順の確立**: そのバグを100%再現するための最小限の手順（ミニマルコード、特定の入力データ）を特定する。

### 2. 推論と仮説検証 (Hypothesis & Verification)
修正コードを書き始める前に、必ず「なぜそのエラーが起きているのか」の仮説を立てます。
* **仮説の構築**:
  - 「〇〇のタイミングでデータがNullになっているのではないか」
  - 「Windows特有のソケットAPI仕様や非同期タイミングによるものではないか」
  - 「GPU JITコンパイル時のレジスタ圧迫やWork-Groupサイズ不一致によるものではないか」
  などを複数立てる。
* **検証の実行**:
  - ログの出力（`console.log` / `print` / デバッガ等）、または状態のモックを行い、立てた仮説が正しいかどうかを1つずつ検証する。
* **推論プロセスの記録**:
  - 「何を試し、何が違ったか」「なぜこの修正が有効か」のログを整理してユーザーに提示する。

### 3. 修正の適用と退行（デグレード）防止 (Regression Prevention)
* **最小限の修正**: バグを直すためだけの最もシンプルでクリーンな修正を心がけ、ついでに他の無関係なリファクタリングを同時に行わない。
* **多層防御 (Defensive Programming)**:
  - 入力側（GUI）でのバリデーションだけでなく、受取側（Core/Worker）でもフォールバックデフォルト値を設定し、想定外の入力でもクラッシュさせない。
* **副作用の考慮**: その修正が、他のコンポーネントや依存関係（マルチGPUディスパッチ、Web監視ダッシュボード、アイドル検知）に影響を与えないかをレビューする。
* **再発防止策**: 修正が完了したら、同様のバグが二度と発生しないようにするための単体テストの追加、または `diagnose.py` への自己診断ステップの組み込みを実施する。

### 4. AI へのデバッグ依頼時のプロンプト作法
ユーザーがデバッグの支援を求める際は、以下の情報を提示して思考を同期させます。
1. **コンテキスト**: 実行しようとしていることの目的と、現在のコード全体（または関連する関数）。
2. **エラーの詳細**: スタックトレース、エラーメッセージの全文、または期待される挙動と実際の挙動。
3. **試したこと**: すでに検証した仮説や、試して効果がなかったアプローチ（AIが同じ提案を繰り返すのを防ぐため）。

---

## Ⅲ. プロジェクト固有の技術規約 (Architecture Conventions)

本プロジェクト（MonaMiner RTX / RX）固有の実装規約です。

1. **外部実行ファイル依存ゼロの維持**:
   - 原則としてサードパーティ製マイナー（`ccminer.exe` や `wildrig.exe`）の同梱・必須化を行わず、Windows標準の `OpenCL.dll` + `ctypes` による独自内蔵エンジンで完全動作させる。
2. **JIT OpenCL カーネルの最適化維持**:
   - ループ処理には `#pragma unroll` を適用し、ハードウェアALU稼働率を最大化する。
   - コンパイラオプションには `-cl-mad-enable -cl-no-signed-zeros -cl-fast-relaxed-math` を標準指定する。
   - 局所ワークグループサイズは `128` を標準とし、各GPUアーキテクチャの占有率（Occupancy）を崩さない。
3. **非同期アーキテクチャの遵守**:
   - 採掘ループ（`OpenCLMinerWorker`）、Stratum通信（`StratumClient`）、Web監視（`WebMonitoringServer`）、アイドル検知（`IdleTracker`）はすべてQtメインスレッドから分離（QThread / threading.Thread）し、GUIの応答性を絶対に損なわない。
4. **ポータブル版パッケージの一貫性**:
   - バージョンアップ時は必ず `main.py`, `version_info.txt`, `app/ui/main_window.py`, `build_portable.py`, `README.md` のバージョン表記を完全同期させ、配布用ZIP（`dist/MonaMinerRTX_Portable_v*.zip`）をビルド＆同梱すること。
